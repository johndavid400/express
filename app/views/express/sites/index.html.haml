%h1.left Listing Sites
= link_to "#{fa_icon('plus', class: 'green')} New Site".html_safe, express.new_site_path, :class => "right btn btn-default"

%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Name
      %th Slug
      %th

  %tbody.list
    = render 'express/sites/index', list: @list

